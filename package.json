{
  "name": "pdf-generator-invoice",
  "version": "1.0.0",
  "description": "A simple pdf generator for invoices",
  "main": "pdf-generator.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "handlebars": "^4.7.7",
    "puppeteer": "^1.20.0"
  }
}
